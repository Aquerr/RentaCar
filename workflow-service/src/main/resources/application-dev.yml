# Datasource
spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:rentacar_workflow;
    username: rentacar_workflow
    password: rentacar_workflow

management:
  endpoints:
    web:
      exposure:
        include:
          - health
          - flowable